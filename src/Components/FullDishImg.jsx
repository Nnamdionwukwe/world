/* eslint-disable react/prop-types */
// eslint-disable-next-line react/prop-types
export default function FullDishImg({ selectedId }) {
  console.log(selectedId);
  //   const { image, price } = selectedId;

  return <div>Full Image</div>;
}
